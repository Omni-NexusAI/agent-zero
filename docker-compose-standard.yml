name: agentspine-standard

services:
  a0-standard:
    container_name: agentspine-standard
    build:
      context: ./docker/run
      dockerfile: Dockerfile
      args:
        # Default stays on the standard pre-release build tag; override with env as needed.
        GIT_REF: ${GIT_REF:-v0.9.9-standard-pre}
        BUILD_VARIANT: standard
        RELEASE_CHANNEL: ${RELEASE_CHANNEL:-pre}
        PYTORCH_VARIANT: cpu
        CACHE_DATE: ${CACHE_DATE:-none}
    ports:
      - "8895:80"
    environment:
      - BUILD_VARIANT=standard
      - RELEASE_CHANNEL=${RELEASE_CHANNEL:-pre}
      - PYTORCH_VARIANT=cpu
      # When true, standard exposes the remote worker option + defaults
      - A0_ENABLE_REMOTE_TTS=${A0_ENABLE_REMOTE_TTS:-false}
    restart: unless-stopped

  kokoro-gpu-worker:
    profiles: ["worker"]
    container_name: agentspine-standard-kokoro
    build:
      context: .
      dockerfile: docker/Dockerfile.kokoro
      args:
        CACHE_DATE: ${CACHE_DATE:-none}
    ports:
      - "8896:8891"
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    restart: unless-stopped
